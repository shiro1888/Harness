# DeepSeek Harness 一键安装器

只需要下载 **1 个文件：dsh-setup.bat**。双击后会自动准备便携 Node.js、安装或更新 DeepSeek Harness，并启动网页界面。整个过程不需要 Git、不需要管理员权限，也不会替换电脑原有的 Node.js。

## 最简单的用法

1. 把 dsh-setup.bat 下载到机房电脑。任选一种方式：

   - 浏览器打开：

         https://gh.shiro1888.com/https://raw.githubusercontent.com/shiro1888/Harness/main/dsh-setup.bat

   - 在命令提示符中执行：

         curl -L --create-dirs -o "%USERPROFILE%\dsh\dsh-setup.bat" "https://gh.shiro1888.com/https://raw.githubusercontent.com/shiro1888/Harness/main/dsh-setup.bat"

2. 双击 dsh-setup.bat。终端会用中文显示当前步骤，安装完成后自动启动网页。

## 每次运行会做什么

1. 首次运行时，自动查询并下载适合当前电脑的最新 Node.js LTS 便携版。以后复用这个运行时，不修改系统环境。
2. 同时查询 npm 官方仓库和国内镜像的 @deepseek-ai/dsh latest 标签。
3. 如果本地不是最新版，自动安装或更新；如果已经是最新版，直接跳过下载。
4. 安装后同时核对包版本、dsh.cmd、dsh --version、Koffi 原生模块和 Windows 伪终端，校验通过才会启动。
5. 默认使用端口 3080；如果被占用，会在 3081 到 3099 中自动选择一个空闲端口。

latest 始终以 npm 官方标签为准。即使它目前是带 rc 的候选版本，只要 DeepSeek 官方把它标记为 latest，安装器就会跟随；脚本没有永久固定某个 dsh 版本。国内镜像已同步同一版本时优先从镜像下载，镜像落后时自动改用官方仓库。

## 可选命令

只安装或更新并校验，不启动网页：

    dsh-setup.bat --install-only

指定端口：

    dsh-setup.bat --port 3081

启动服务但不自动打开浏览器：

    dsh-setup.bat --no-open

用于自动化脚本，不等待按回车：

    dsh-setup.bat --install-only --no-pause

## 文件和配置范围

- 便携 Node.js、dsh 和安装缓存都放在脚本旁边的 dsh-runtime 文件夹。
- 安装器使用自己的 npm 配置文件，不会把国内镜像写入用户的 %USERPROFILE%\.npmrc。
- npm 安装脚本只对白名单中的 DSH 依赖放行；出现新的未核查安装脚本时会停止，而不会把全局安全策略关闭。
- 已有便携 Node.js 能正常运行时会继续复用；dsh 仍会在每次运行时检查更新。
- DeepSeek Harness 自己创建的用户配置和会话数据由它管理，更新安装器不会主动删除这些数据。

## 常见问题

### 下载失败

安装器会在国内镜像和官方地址之间自动回退。如果两个地址都失败，请检查网络、代理、防火墙和系统时间后重试。

### npm 官方仓库暂时无法访问

如果国内镜像可用，安装器会暂时按镜像的 latest 安装；下次运行会再次核对官方版本。如果本机已有可用 dsh，但两个仓库都暂时无法访问，安装器会保留并启动现有版本。

### 浏览器没有自动打开

保留黑色终端窗口，在输出中找到包含 ?token= 的完整地址，复制到浏览器地址栏。只输入 http://127.0.0.1:端口 可能缺少登录令牌。

### 想彻底重新安装便携运行时

先关闭 DeepSeek Harness，再删除脚本旁边的 dsh-runtime 文件夹，然后重新双击安装器。不要删除自己仍需保留的 DeepSeek Harness 用户配置或会话目录。
