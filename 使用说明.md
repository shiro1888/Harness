# DeepSeek Harness 一键安装包（机房 / 无环境版）

## 这是什么

在「没有任何环境」的 Windows 电脑上，双击一下就把 DeepSeek Harness（`dsh`）装好并启动。它：

- **不需要 git**
- **不需要管理员权限**（用的是绿色便携版 Node.js）
- **不需要访问 GitHub**（全程走国内镜像：淘宝 npmmirror）
- 装完自动打开浏览器进入 Web 界面，默认地址 `http://127.0.0.1:3080`

## 原理（一句话）

这个项目官方直接发布了 npm 包 `@deepseek-ai/dsh`。我们只需要一个 Node.js，然后从**国内 npm 镜像**把它装上即可，全程不碰 GitHub、不碰 git。

## 使用步骤

1. 把下面三个文件放进**同一个文件夹**（U 盘或 D 盘都行），建议用**英文路径**，例如 `D:\dsh`，避免中文路径偶发问题：
   - `一键安装-DeepSeek-Harness.bat`
   - `install-dsh.ps1`
   - `启动-DeepSeek-Harness.bat`

2. **双击** `一键安装-DeepSeek-Harness.bat`
   - 第一次会联网下载便携版 Node.js（约 30 MB）和 dsh 本体（几百 MB 依赖），需要几分钟，请耐心等待。
   - 装完自动启动并打开浏览器。

3. 以后每次想用，**双击** `启动-DeepSeek-Harness.bat` 即可（秒开）。

## 装完后要做什么

第一次打开网页后，需要先配置模型 API Key（DeepSeek 官方 key，或任意 OpenAI 兼容接口）才能对话。在网页界面里添加即可。

## 常见问题

- **提示下载失败 / 网络错误**：机房可能拦了某些地址，重新双击跑一次；还是不行就把脚本里的镜像换成别的（见下方备选）。
- **杀毒软件拦截**：放行 `curl.exe` / `powershell.exe`，或临时关闭实时防护。
- **端口 3080 被占用**：把 `启动-DeepSeek-Harness.bat` 里的 `dsh web` 改成 `dsh web --port 3081`。
- **双击没反应 / 窗口一闪而过**：可能被安全策略拦了脚本，右键「以管理员身份运行」，或用 PowerShell 手动执行 `install-dsh.ps1`。

## 环境要求

- Windows 10 / 11（64 位），能上网
- 磁盘预留约 1 GB
- Node.js 要求 22.19+ 或 24+（脚本会自动选最新 LTS，满足要求）

## 备选方案：源码版（想读/改代码时用）

需要 git + pnpm + 从 GitHub 拉代码（国内用镜像加速）：

```
git clone https://gitclone.com/github.com/deepseek-ai/deepseek-harness.git
cd deepseek-harness
npm install -g pnpm --registry=https://registry.npmmirror.com
pnpm install
pnpm run build
pnpm dsh web
```

GitHub 加速镜像（失效就换下一个）：
- `ghproxy.net` / `gh-proxy.com`（文件与 clone 加速）
- `gitclone.com`（clone 加速，最稳）
- `ghp.ci`（Release/压缩包加速）
- `kkgithub.com` / `bgithub.xyz`（网页浏览）

## 更懒的一条命令（如果某台机器已经装了 Node.js）

```
npm install -g @deepseek-ai/dsh --registry=https://registry.npmmirror.com
dsh web
```
