# DeepSeek Harness 一键安装（单文件版）

只需要 **1 个文件**：`dsh-setup.bat`，双击即自动装好并启动。不需要 git、不需要管理员、不需要直连 GitHub。

## 用法（就两步）

1. 把 `dsh-setup.bat` 弄到机房电脑（任选一种）：
   - 浏览器打开（走你自己的加速站）：
     `https://gh.shiro1888.com/https://raw.githubusercontent.com/shiro1888/Harness/main/dsh-setup.bat`
   - 或命令行：
     `curl -L --create-dirs -o "%USERPROFILE%\dsh\dsh-setup.bat" "https://gh.shiro1888.com/https://raw.githubusercontent.com/shiro1888/Harness/main/dsh-setup.bat"`

2. 双击 `dsh-setup.bat`，自动下载 Node、装 dsh，并启动网页（http://127.0.0.1:3080）。

以后同一台电脑再用：双击同一个 `dsh-setup.bat` 即可（会自动跳过已装部分，直接启动）。

## 说明

- 第一次会从国内镜像 npmmirror 下载 Node（约 30 MB）和 dsh 依赖（几百 MB），需几分钟。
- 首次打开网页后，需在界面里填模型 API Key 才能对话。

## 常见问题

- 下载失败：检查你的加速站 `gh.shiro1888.com` 是否在线；或临时换成公共镜像 `gh-proxy.com`。
- 端口 3080 被占用：把脚本最后一行 `dsh web` 改成 `dsh web --port 3081`。
